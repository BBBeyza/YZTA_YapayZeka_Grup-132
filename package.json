{
  "name": "project-starter",
  "version": "1.0.0",
  "description": "Start Flutter frontend and Python backend simultaneously",
  "scripts": {
    "build": "concurrently \"flutter build web\" \"cd backend && pip install -r requirements.txt\"",
    "start": "concurrently -k \"cd frontend && flutter run\" \"cd backend/python && start python spiral_app.py\" \"cd backend/python && start python kisametintesti.py\""  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}